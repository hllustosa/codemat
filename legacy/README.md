# Legacy archive

A full copy of the pre-revamp `webapp/` and `backend/` may exist locally under
`legacy/archive/` (gitignored). Curriculum sources used by the build live in:

- `legacy/exercises`
- `legacy/classes`

Active code is only under `apps/`.
