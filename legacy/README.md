# Legacy archive

Curriculum sources used by the content build:

- `legacy/exercises` — exercise JSON / JSX statements
- `legacy/classes` — lesson JSX modules

## Resources

- [`legacy/resources/compemat-latex.zip`](resources/compemat-latex.zip) — LaTeX course material (source + images), archived from the previous `compemat-latex` project. Not used by the web app build.

A full copy of the pre-revamp `webapp/` and `backend/` may exist locally under `legacy/archive/` (gitignored).

Active application code is only under `apps/`.
