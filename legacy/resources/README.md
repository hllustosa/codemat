# Legacy resources

Archived materials that are **not** consumed by the Next.js / Functions apps.

| File | Description |
|------|-------------|
| `compemat-latex.zip` | Full LaTeX course (`main.tex` + `Imagens/`) |

Unzip locally if you need to rebuild or edit the printable course notes.
